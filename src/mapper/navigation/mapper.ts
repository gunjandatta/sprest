import { RequestType } from "../../types";

/**
 * Navigation
 */
export const navigationservicerest = {
    /**
     * Properties
     */
    properties: [
        "MenuState|menustate|([Name])|menunode"
    ]

    /**
     * Methods
     */
}
